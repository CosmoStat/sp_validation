# Committed SLURM profile for the candide cluster (IAP).
#
# Drive any target with
#
#     snakemake --profile workflow/profiles/candide \
#         -s workflow/image_sims/Snakefile \
#         --configfile <run config> <target>
#
# Snakemake owns scheduling (one SLURM job per branch x tile) and the
# container: every rule runs through Snakemake's own container wrapping
# (``container:`` on the rule or the module-level default in
# workflow/Snakefile), never a rule's own ``apptainer exec`` shell call.
# ``software-deployment-method: apptainer`` below turns that wrapping on;
# ``apptainer-args`` carries the bind mounts every rule needs. The one
# exception (``covariance_cosmocov``) is documented at its rule definition in
# workflow/rules/.
#
# ``snakemake`` itself is a thin host-side tool, pinned via ``uv tool
# install`` (see workflow/README.md); run it on the host, never inside an
# ``apptainer shell``.

executor: slurm

# Kept in sync with the ``app`` bash function's raw ``apptainer exec`` in the
# top-level UNIONS CLAUDE.md -- update both together.
software-deployment-method: apptainer
apptainer-args: "--cleanenv --bind /home,/scratch,/automnt,/n17data,/n23data1,/n09data"

# Shared image cache. The entry Snakefiles name a GHCR *tag*; Snakemake pulls it
# here on first use (once, ~15 min, on the host running snakemake) and every
# later run reuses the cached SIF. Shared rather than per-run-directory
# (Snakemake's default is ``.snakemake/singularity``) so one pull serves every
# workflow and every checkout.
apptainer-prefix: /n17data/cdaley/containers/snakemake-sif

# Cluster policy applied to every job unless a rule overrides it. Excludes are
# the flaky/no-internet candide nodes (n17 mount issues, n09 no internet, n36).
#
#   * ``runtime`` MUST carry a unit (``60m``, ``6h``, ``2d``). Snakemake's
#     resource parser reads a bare number as SECONDS, so ``runtime: 60`` would
#     silently give every job a 60-second wall clock and kill it on start.
#     (A bare integer in a rule's own ``resources: runtime=720`` is fine --
#     rule-level numeric runtime is read as minutes; the seconds trap is only
#     the CLI/default-resources parser.)
#
#   * ``cpus_per_task`` is pinned to 12 to CAP JOBS PER NODE, not because a job
#     needs 12 cores. candide's per-user process limit is ``ulimit -u 1200``
#     per node, and apptainer crashes ("can't start new thread") beyond ~4
#     concurrent jobs on a 48-core node; 12 CPUs/job holds SLURM to ~4 jobs per
#     node.
#
#   * After launching a real fan-out, verify the request landed:
#     ``squeue -u $USER -o "%C %l"`` should show 12 (CPUs) and the intended
#     wall clock.
default-resources:
  slurm_account: "cusers"
  slurm_partition: "comp,pscomp"
  runtime: "60m"
  cpus_per_task: 12
  slurm_extra: "'--exclude=n17,n09,n36'"

# Give an appearing output file a moment on candide's automounted filesystems
# before Snakemake calls a job failed for a missing output, and retry a job
# once on transient node failure.
latency-wait: 5
retries: 1

# Keep the SLURM logs of successful jobs (candide debugging), and rerun a job
# when its code / params / inputs change, not only on mtime.
slurm-keep-successful-logs: true
rerun-triggers: ["mtime", "params", "input", "code"]
